# Tank3D 项目执行规则

- 总方案经用户批准后，Agent 应连续完成设计、实现、测试与验收，不得对同一方案重复索要“批准设计”或“批准执行”。
- 仅当需求发生重大冲突、需要扩大授权范围或存在不可逆风险时，才暂停并提交 `REPORT_CONFLICT`。
- 所有新行为遵循 TDD：先观察预期失败，再实现并回归。
- 默认使用隔离无头浏览器验证，不连接用户主浏览器。
- 背景铺满与缩放问题须核对视口、容器、canvas 显示尺寸及绘图尺寸，并覆盖小于 1 和大于 1 的像素比例及窗口 resize；不得凭底色一致或单张默认尺寸截图判定修复。
- 文本源码使用 UTF-8 无 BOM；修改人统一记录为 Unclecow。

## AI Failure Memory

- 移动适配不得凭 maxTouchPoints 或窗口宽度启用；交付前用普通桌面实际入口验证三个游戏均无移动控件，并覆盖非零触点的桌面环境。浏览器能力值不能作为用户硬件事实。
