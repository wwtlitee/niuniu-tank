const {chromium}=require('playwright'),fs=require('fs'),assert=require('assert/strict');
(async()=>{const b=await chromium.launch({headless:true,args:['--use-angle=d3d11']});const out='output/hero-workshop-v7.2.0';fs.mkdirSync(out,{recursive:true});try{const p=await b.newPage({viewport:{width:1280,height:800}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:8001/play/niuniu-tank/index.html?mode=survival&autotest=1');await p.waitForFunction(()=>typeof assetsReady==='function'&&assetsReady()&&state===7,null,{timeout:120000});
console.log(await p.evaluate(()=>{state=STATE.PAUSED;game.gold=1e8;game.popMax=100;const spec=shopList().find(b=>b.id==='heroHub');for(let z=3;z<GRID-4&&!heroHubs.length;z++)for(let x=3;x<GRID-4;x++){if(!footprintPlaceable({x,z},spec))continue;selectBuild(shopList().indexOf(spec));ghostCell={x,z};ghost.visible=true;placeBuildingImmediately(null,null,null,true);selectBuild(null);if(heroHubs.length)break;}const h=heroHub();camFocus.set(h.group.position.x,0,h.group.position.z);camHeight=28;updateCamera(1);produceHero();return {hub:h.group.position.toArray(),route:findHeroDeployment(h)};}));
const metrics=await p.evaluate(async()=>{const results=[];for(const phase of ['assembly','courier']){if(phase==='courier'){for(let i=0;i<2400&&!heroTank;i++)updateHeroProduction(1/60);buyHeroSkill('rail');}const frames=[];let last=performance.now(),calls=0,triangles=0;for(let i=0;i<600;i++){await new Promise(requestAnimationFrame);const now=performance.now();frames.push(now-last);last=now;updateHeroProduction(1/60);renderer.render(scene,camera);calls=Math.max(calls,renderer.info.render.calls);triangles=Math.max(triangles,renderer.info.render.triangles);}frames.sort((a,b)=>a-b);results.push({phase,frames:frames.length,median:frames[300],p95:frames[570],p99:frames[594],calls,triangles,memory:{...renderer.info.memory}});}const gl=renderer.getContext(),ext=gl.getExtension('WEBGL_debug_renderer_info');return {gpu:ext?gl.getParameter(ext.UNMASKED_RENDERER_WEBGL):'unavailable',metrics:results};});fs.writeFileSync(out+'/performance.json',JSON.stringify(metrics,null,2));console.log(metrics);assert.deepEqual(errors,[]);}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1});