# 2026-08-26 工作日志

## 坦克大战3D - 设计增量落地（P0：防守张力）

### 用户方向
- 「先做设计增量，再补防守张力/平衡/长远目标」
- 设计增量蓝图三板块：防守张力 / 数值平衡 / 长远目标

### 已实现（P0）
1. **构筑物血条**：机枪炮台 14 血 / 重炮炮台 22 血，受击后头顶显示血条（PlaneGeometry 血条，lookAt 镜头），绿色按比例缩短。
2. **敌人攻击构筑物**：
   - 普通兵/精英撞墙后「啃咬」墙体（1 伤害/0.9s），BOSS 3/0.55s、重装 2 伤害。
   - 玩家放置的钢墙有 8 点耐久（`steelHP` Map），可被拆毁；地图原生钢墙不可破坏。
   - 重装兵/BOSS 主动侦测最近炮台（重装射程 6.5/侦测 10，BOSS 9/18），进入射程停车强拆，伤害直接结算，摧毁后 toast 提示。
3. **波间构筑倒计时**：45 秒，倒计时归零自动开波；≤10 秒数字变红。

### 关键实现
- `damageWallCell(cx,cz,dmg)`：砖墙直接碎，钢墙走 `steelHP` 耐久表。
- `updateStructBars(t)` / `destroyTurret(t)` 辅助函数。
- `updateEnemies` 内 `holdFire` 强拆逻辑（锁定炮台时 `e.dir.set(0,0,0)` 防止朝向回拽）。

### 验证
- node 语法 OK。
- agent-browser 实测：`showBuildPhase()` 倒计时 45 显示正常；炮台放置成功（60 金，血条可见）；构筑 UI DOM 正常。
- 注意：agent-browser daemon 跨回合保持页面运行，倒计时会在回合间隙真实走完，验证时需先重新 `showBuildPhase()`。

### 待办（用户新方向）
- 用户提出三模式架构：经典玩法（扩张地图+防御工事之前的旧版）/ 无尽玩法（当前版）/ 塔防玩法（固定路径刷怪+摆塔+坦克位升级）。
- 旧版代码未完整保存：`.tmp/_check.js` 是「大地图 GRID 47 + 无构筑」中间版，经典模式需参数化还原（GRID 31 小地图 + 高密度障碍 + 无构筑）。

---

## 坦克大战3D - 三模式架构与主菜单美化

### 已完成
1. **主菜单纯代码视觉升级**：
   - SVG 背景层：径向渐变迷雾、网格、城市剪影、双坦克剪影、探照灯光束、爆炸辉光、随机闪烁光点。
   - 标题金属渐变 + `background-clip:text` + 脉冲光晕。
   - 模式卡片入场 stagger 动画、悬浮边框流光、`--mcGlow` 主题色发光。
   - 不使用任何外部图片，headless Chrome 截图验证通过。
2. **经典模式参数化还原**：
   - `GAME_MODES.classic`：GRID 31、 streets [5,15,25]、高密度障碍、无构筑、cameraY 42 / cameraZ 30。
   - `applyModeConfig` 动态重建地面/网格/阴影相机范围；`resetGame` 立即归位相机。
   - headless Chrome 验证可正常进入 Wave 1，视角与地图尺寸匹配。

### 进行中
- 无尽模式改造为幸存者模式：连续刷怪、B 键随时打开构筑商店、每波自动推进、周期性三选一强化、敌人随波次指数成长。

### 验证技巧
- headless Chrome 截图命令（需每次用新 profile 避免锁）：
  `chrome --headless --disable-gpu --user-data-dir=E:/坦克大战3D/.tmp/chrome-profile --screenshot=... --virtual-time-budget=6000 --run-all-compositor-stages-before-draw file:///E:/坦克大战3D/index.html?mode=classic&autotest=1`
