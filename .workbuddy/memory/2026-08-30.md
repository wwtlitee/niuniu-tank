# 2026-08-30 坦克大战3D

## P5 用户反馈轮：美观+平衡+尸潮+怪物（进行中）

用户评价 P4「初具雏形但 bug 多」，附截图（wc3tip 空指针刷屏）。5 大意见：
1. 不美观、粗糙（一二层地板感觉全没做）
2. 所有系统需打磨，平衡要研究（初期无敌滚雪球）
3. 敌人建模要换怪物（设计稿给过）
4. 敌人数量大幅提升（尸潮，性能允许前提）
5. 让我自己截图自己玩自己改

### 已实装（engine.js / config.js / index.html）

1. **wc3tip 修复**：index.html 补 `<div id="wc3tip">`（只有 CSS 没 DOM）；
   renderCmdCard hover 两处加 null 防御。截图验证 tooltip shown:true。
2. **怪物模型**：assets/monsters/ 新增 graveyard-kit 5 模型（zombie/skeleton/vampire/ghost/keeper，
   全带 idle/walk/sprint/die/attack-melee-* 动画，与 blocky-characters 同构）。
   ENEMY_CHAR_MAP 重映射：normal=丧尸 fast=幽灵 heavy=吸血鬼 sniper=守墓人 boss=骷髅(×2.0)。
3. **地板铺装**：3d-road-tiles（gltf 内嵌 base64 自包含）→ 自写 gltf2glb 转换器
   （js Buffer 拼 GLB header，注意 TextEncoder 输出是 Uint8Array 不是 Buffer）。
   assets/floors/ 5 砖（grass/grassB/grassC/stone/stoneB）。
   addGroundFloorTile：T_EMPTY 格哈希混铺（70/12/8/6/4），1.08 缩放消缝、-0.02 下沉、
   90° 随机旋转；_batchFlush 对 floor- 前缀 clone 材质压暗染色（0x9db77a×(0.55,0.62,0.5)，stone 0x6f7a6e），
   castShadow 关（同 tile-high）。
4. **尸潮**：enemiesPerWave=10+n*4+n^1.6（原 6+n*1.6）；spawnCap=min(12+w*3,60)（原 14 上限）；
   spawnInterval 最低 0.18s；difficultyScale=1.12^w 复利（原线性 0.10）；
   pickEnemyType 丧尸权重 10+w*2 永远主力。
5. **经济压制**：金矿 tiers [8,11,15,20,26,33]（原 14..78）、升级价 1.55（原 1.35）、
   killGold 3（原 4）、mineCost 90、waveClearBonus 20+w*4、houseCost 60、techTowerCost 120。
   参考网上 TD 经济设计：收入≈理想支出 60-70%，负反馈（难度复利）须略强于正反馈（滚雪球）。

### 截图自查（diag-p5.mjs，8030 端口）

- 8030 独立起服（8001 被别的项目占了！8000=牛牛的结合，8001=牛牛游戏厅，都不动）。
- 资产全就绪、tooltip 修复生效、15 敌同屏（第 1 波 toSpawn 走完）。
- 首轮截图地板亮绿斑驳 → 已修（压暗+消缝+下沉），修复后截图绿色统一暗草色，视觉可接受。
- p5-build.png：建造模式命令卡 10 格 + tooltip 正常。
- 遗留小问题：drawMode console warning（Kenney 模型带 fan 折面，r128 警告但渲染正常，不修）。

### 待办（下一轮）

- 再对比 screenshot 确认台面/崖壁视觉、尸潮波（W5+）性能帧率
- diag-p5 断言化（enemies 数量阈值、资产失败数=0 中 fails:5 需查——5 个失败可能是旧 characters 包？未确认）

## P5b 素材库迁移 + 地形重拼（完成）

用户把 Kenney 全库搬到 `E:\GameHub\kenny`（raw 195 包 / zips / index\catalog.csv / models / ui / audio，
CC0，README 有检索说明）。原 `E:\kenny\raw` 仍可用。

### 素材修复
- characters/Textures/texture-*.png 是 67B 坏 PNG（"source image could not be decoded"），
  从 blocky-characters 源包重拷 5 张真纹理（13~22KB）后加载成功。**教训：GLB 外部纹理占位文件会静默毁掉模型加载**。

### 新接入两个包（assets/platformer/ 40 文件 + assets/tdkit/ 24 文件，各带 Textures/colormap.png）
- **platformer-kit**：block-grass 圆角草块系（foot≈1.08 格中心原点）。
- **tower-defense-kit tile 系**：1×1×0.2 草皮方砖 + bump/dirt/rock/hill/tree 变体。

### 地形重拼（engine.js buildMapMeshes）
- 一层地面 addGroundFloorTile v2：TD tile 系哈希混铺（tile 80%/bump 8%/dirt 5%/rock 3%/hill 2%/tree 2%）。
- 台面 addPlateauTile v2：block-grass 等比缩放顶面对齐 PH（回退 tile-high 薄板）。
- 崖壁 addCliffRock v2：block-grass-low（0.5 高草块堆叠），放弃 cliff_block_rock。
- 坡道 T_RAMP v2：block-grass-large-slope（h0.759×2.08 foot），不足 step 时垫 block-grass-low 补差。
- 台面装饰 decorateSurvivalCliff v2：platformer 同族 rocks/flowers/mushrooms/crate/barrel/sign（color 传 0xffffff，placeModel 不染真模型）。
- 生存地面色 0x1d2b22→0x3d5a3a（草色统一）。

### 截图目验（smoke/out/p5-plateau.png 09:49 / p5-horde.png 09:54）
- 台面草块圆角质感受到认可方向；地面 TD 草砖整齐自然。
- 资产 fails:0、tooltip OK、命令卡 OK。
- 已知渲染瑕疵：崖壁草块 0.5 高不够 2.2 需两块叠——当前实现只放一块顶对齐 PH（柱身拉高? 否：等比放大到 1.04 格宽则高≈2.2 恰好）实际 block-grass-low h0.5×(2.2/0.5) 变形风险——**后续轮需近景复查崖壁比例**。

### 环境
- 8000=牛牛的结合、8001=牛牛游戏厅（GameHub）、**8030=坦克大战3D 独立服**（npx http-server）。
- node -e 内联 playwright 在 bash 管道下无声挂死 → 一律写 smoke/*.mjs 脚本文件跑。
